# automeals - backend

The backend of `automeals` is built using `React`. 

## Installation

```bash
yarn install
```

## Development

- Run a local, development version of the frontend:

```bash
yarn dev
```