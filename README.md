# automeals

A full-stack application that semi-automates my weekly meal plan. The frontend is built in React and the backend is built in python using fastapi.

## Usage

The frontend can be initialised using `yarn dev` and the backend should be deployed using `unicorn`
